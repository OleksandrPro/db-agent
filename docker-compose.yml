services:
  agent:
    build:
      context: .
      dockerfile: src/Dockerfile
    environment:
      - PYTHONPATH=/app/src
    env_file:
      - .env
    depends_on:
      - db-test
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  db-test:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: ${DB_TEST__USER}
      POSTGRES_PASSWORD: ${DB_TEST__PASSWORD}
      POSTGRES_DB: ${DB_TEST__NAME}