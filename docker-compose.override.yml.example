services:
  agent:
    volumes:
      - ./src:/app/src
    environment:
      - DB_PROD__HOST=db-prod
      - DB_TEST__HOST=db-test
    depends_on:
      - db-prod
    stdin_open: true
    tty: true

  db-prod:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: ${DB_PROD__USER}
      POSTGRES_PASSWORD: ${DB_PROD__PASSWORD}
      POSTGRES_DB: ${DB_PROD__NAME}
    ports:
      - "${EXTERNAL_PROD_PORT}:5432"
    volumes:
      - prod_data:/var/lib/postgresql/data

  db-test:
    ports:
      - "${EXTERNAL_TEST_PORT}:5432"

volumes:
  prod_data: